# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: weather-mcp-server-azd
metadata:
  template: weather-mcp-server-azd@1.0.0
services:
  api:
    project: .
    language: js
    host: function
